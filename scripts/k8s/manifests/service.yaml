apiVersion: v1
kind: Service
metadata:
  name: chainbreaker
  labels:
    app: chainbreaker
spec:
  type: ClusterIP
  selector:
    app: chainbreaker
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
