apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: chainbreaker-home-pvc
  labels:
    app: chainbreaker
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
